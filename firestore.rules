rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now
    // This is open for development - should be restricted in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
